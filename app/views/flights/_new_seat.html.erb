<%= form_with(model: seat, local: true) do |form| %>
  <% if seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
      <% seat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<h1> Add Seat </h1>

<div class="row">
  <div class="col-sm-4">
        <div class="card">
          <div class="card-body">

          <div class="field">
            <%= form.label :name %>
            <%= form.text_field :name, class: "form-control" %>
          </div>

          <div class="field">
            <%= form.label :baggage %>
            <%= form.text_field :baggage, class: "form-control" %>
          </div>

          <%= form.hidden_field :flight_id, value: f_id%>

          <div class="actions">
            <%= form.submit :Add, class: "btn btn-warning btn-block" %>
          </div>
        <% end %>
        </div>
    </div>
  </div>
</div>

<br>